/// Defines the visual styling variant of the [MechanixListTile].
enum ListTileVariant {
  /// Standard list item with a transparent background.
  standard,

  /// Segmented card list item with filled container background.
  segmented,
}
